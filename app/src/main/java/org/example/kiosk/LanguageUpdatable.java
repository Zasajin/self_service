package org.example.kiosk;

/**
 * Interface for components that can update their language.
 */
public interface LanguageUpdatable {

  /**
   * Update the langauge after a language change (langButton clicked).
   */
  void updateLanguage();
}
